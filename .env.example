# PostgreSQL connection string
# Replace with your own PostgreSQL instance (local, Neon, Supabase, AWS RDS, etc.)
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<database>?sslmode=require

# Server port
PORT=3000
